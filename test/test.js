/**
 * assemble-plugin-assemble-drafts
 */

var expect = require('chai').expect;
var plugin = require('../');

describe('assemble-plugin-assemble-drafts', function() {

	before(function(){
		// run any code before tests here
	});

  it('should do something awesome', function() {
    var expected = 'assemble-plugin-assemble-drafts';
    var actual = 'assemble-plugin-assemble-drafts';
    expect(actual).to.eql(expected);
  });
});